# Copy this to .env.local and fill in your values.
# Leave VITE_API_BASE_URL blank (or remove it) to use local IndexedDB storage.
# Set it to your API server address to enable remote sync.

VITE_API_BASE_URL=
